{
  "name": "letter-app",
  "version": "1.0.0",
  "description": "Letter App - A full-stack application",
  "scripts": {
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "nodemon": "2.0.22"
  }
} 